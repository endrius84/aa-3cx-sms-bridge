# Port the bridge listens on
PORT=3000

# Path A&A will POST inbound SMS to (mounted under your public URL)
INBOUND_PATH=/aa-sms-in

# Path 3CX will POST outbound send-requests to (mounted under your public URL)
OUTBOUND_PATH=/cx-sms-out

# Your 3CX Generic SMS provider webhook URL (Admin Console -> Messaging -> Add Provider -> Generic)
THREECX_WEBHOOK_URL=https://xxxx.3cx.cloud/sms/generic/xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# A&A's outbound SMS gateway (default is correct, only change if A&A tells you otherwise)
AA_SMS_URL=https://sms.aa.net.uk/sms.cgi

# A&A VoIP number (username) and SIP/outgoing password from the Control Pages
AA_USERNAME=+441234567890
AA_PASSWORD=your-aa-outgoing-password

# Optional: shared secret appended as ?key=... to the inbound URL you give A&A,
# since A&A's POST has no signature/auth of its own. Generate with:
#   node -e "console.log(require('crypto').randomBytes(16).toString('hex'))"
SHARED_SECRET=
